# Changelog chi tiết — NGS Monitor

> File này chứa **lịch sử chi tiết đầy đủ** (số liệu job thật, log, bug cụ thể, quá trình quyết định)
> đã được rút gọn khỏi `CLAUDE.md` để giảm kích thước file luôn load vào context.
> `CLAUDE.md` chỉ giữ bản tóm tắt 1 dòng/mục — tra cứu chi tiết ở đây khi cần.
> Thêm entry mới ở **đầu file** (mới nhất trước) khi có tiến độ/quyết định lớn.

---

## 2026-07-10 — Bỏ 1 request thừa `latest-news-sitemap.xml` cho job phạm vi quá khứ
Phát hiện qua chính câu hỏi walkthrough với user — `_SITEMAP_ALWAYS_INCLUDE` (VTV) trước đó fetch URL catch-all vô điều kiện, kể cả job có `date_to` hoàn toàn trong quá khứ (chắc chắn không nhận thêm được bài nào vì catch-all chỉ chứa bài mới nhất). Sửa: thêm điều kiện `date_to >= today` trước khi fetch, `today` là tham số injectable (mặc định `date.today()`, cùng pattern DI với `client`/`delay_seconds`/`max_retries` đã có ở `get_article_urls()` — không cần thêm dependency mock/freeze thời gian). Dọn 3 mock handler test đã thêm trước đó cho `latest-news-sitemap.xml` ở 3 test VTV không liên quan tính năng này (nay không còn được gọi tới do gate mới). Thêm test mới `test_vtv_skips_latest_news_sitemap_when_date_range_entirely_in_the_past`. **Đã verify job thật:** job phạm vi `06/06`–`29/06/2026` (quá khứ hoàn toàn) — log xác nhận **không còn** request `GET latest-news-sitemap.xml` (trước đó luôn có). 123/123 test pass.

## 2026-07-10 — Sửa 2 hạn chế sitemap index (VOV tháng hiện tại + VTV mấy ngày gần nhất)
User báo cáo trực tiếp qua quan sát thật (không phải verify job): (1) `vov.vn/sitemap.xml` chỉ thêm entry sub-sitemap của 1 tháng SAU KHI tháng đó kết thúc → job chạy giữa tháng 7 không bao giờ thấy sub-sitemap tháng 7 (chỉ tới tháng 6); (2) `vtv.vn` build sub-sitemap theo khối 5 ngày → vài ngày gần nhất (khối chưa đóng) không nằm trong sub-sitemap nào, không lấy được bài mới nhất. User tự phát hiện 2 URL "lối tắt" hợp lệ qua kiểm tra tay: `https://vov.vn/sitemaps/{year}/{month}/article.xml` (dự đoán được, verify curl trả HTTP 200 dù chưa lên index) và `https://vtv.vn/latest-news-sitemap.xml` (đã có sẵn trong chính index nhưng bị regex `_SITEMAP_DATE_PATTERNS` loại vì không mang ngày tháng trong path). Sửa `sitemap.py`: thêm `_SITEMAP_URL_TEMPLATES` (VOV — bỏ hẳn fetch/parse index, tự sinh URL sub-sitemap cho mọi tháng trong `date_from`–`date_to`, tái dùng `_fetch_declared_sitemap_pages()` đã có) và `_SITEMAP_ALWAYS_INCLUDE` (VTV — luôn fetch kèm URL catch-all cố định, không qua bộ lọc ngày). Xoá 2 test cũ test hành vi index-based của VOV (không còn đúng sau khi đổi cơ chế), thêm 4 test mới (3 cho VOV template, 1 cho VTV always-include) + sửa 3 test VTV hiện có (mock thêm request `latest-news-sitemap.xml`). **Đã verify job thật cả 2 nguồn:** VOV (`job b638f4a4...`, `date_from=2026-07-01`/`date_to=2026-07-10`) → 5/5 bài crawl được, toàn bộ thuộc tháng 7 thật (trước đây sẽ là 0 bài); VTV (`job 5131a68e...`, `date_from=2026-07-09`/`date_to=2026-07-10`) → 5/5 bài, `published_at` toàn bộ đúng `2026-07-10` (hôm nay), log xác nhận `latest-news-sitemap.xml` trả HTTP 200 và được fetch dù không khớp pattern ngày. 122/122 test pass.

## 2026-07-10 — Đổi sang water-filling (bù quota thiếu hụt giữa các nguồn), revision 2
Qua test thật của user ngay sau khi merge (3 nguồn VTV/Vietnam.vn/TinGia cùng ngày, TinGia không có bài) phát hiện thiết kế "không bù thiếu hụt" ban đầu gây lãng phí ngân sách rõ rệt: TinGia không dùng hết quota 2 của nó, nhưng phần dư không chuyển được cho VTV/Vietnam.vn dù 2 nguồn này thừa khả năng lấy thêm (job 2 nguồn không có TinGia, cùng ngày, VTV lấy được 3 bài — chứng minh đủ dữ liệu, chỉ bị quota ban đầu giới hạn ở 1). User yêu cầu đổi sang có bù — sửa `_crawl_sources()`: quota mỗi nguồn tính LẠI ngay trước khi xử lý (`_distribute_evenly(ngân_sách_còn_lại, số_nguồn_chưa_xử_lý)[0]`) thay vì tính 1 lần cố định trước loop, tái dùng đúng `_distribute_evenly()` đã có (không cần hàm mới). Cập nhật spec nội bộ (revision 2), test đổi tên/kỳ vọng tương ứng. **Đã verify job thật** (job `57418b3c-6d0d-4298-a484-3913846d5213`, TinGia/Vietnam.vn/VTV, cùng ngày `2026-07-10`, `MAX_ARTICLES_PER_JOB=5`): kết quả TinGia=0, Vietnam.vn=3, VTV=2, **tổng đúng 5/5** (trước đó chỉ 3/5). 120/120 test pass.

## 2026-07-10 — Chia đều số bài crawl theo nguồn (EVEN_DISTRIBUTE_ACROSS_SOURCES)
Thêm công tắc `EVEN_DISTRIBUTE_ACROSS_SOURCES` (mặc định tắt, giữ hành vi cũ) — khi bật, `_crawl_sources()` chia đều `MAX_ARTICLES_PER_JOB` cho từng nguồn đã chọn theo đúng thứ tự `source_ids` (dư dồn nguồn đầu). Giải quyết vấn đề đã ghi nhận nhiều lần ở Slice 2/3/4 verify: nguồn đầu tiên luôn "ăn hết" ngân sách, không test được đa dạng nguồn với số bài nhỏ. **Đã verify job thật** (job `2078ac9a-6697-4872-a54a-89b70cd9bc56`, 3 nguồn VTV/VOV/vietnam.vn, `MAX_ARTICLES_PER_JOB=5`): kết quả VTV=2, VOV=2, vietnam.vn=1, đúng phân bổ `_distribute_evenly(5, 3) = [2, 2, 1]`, `.docx`/`.json` hợp lệ. 120/120 test pass (113 baseline + 7 mới).

## 2026-07-10 — Slice 4 (Report đầy đủ) hoàn thành
Mở rộng `aggregate_basic()` thêm `source_counts`/`topic_counts`/`keyword_counts`/`monthly_counts`/`summary_stats`, dựng lại `generate_docx()` theo đúng cấu trúc bảng của `sample_report_form.docx` (chỉ bảng khớp dữ liệu thật — bỏ hẳn bảng cần hashtag/engagement/nền tảng social/chính sách cụ thể, không có trong DB; không vẽ chart — chỉ bảng số liệu). Đồng thời sửa bug đã biết từ Slice 3: validate `emotion` AI trả về đúng 6 nhóm enum chuẩn ngay ở `ollama_client.py`, giá trị ngoài enum (VD `"Neutral"`) → `emotion=None` + `needs_review=true` + log warning, không còn lọt thẳng vào DB/report. **Đã verify job thật** (job `14a4c918-9f1e-4192-a815-0ef3b6c8a384`, nguồn VTV, `date_from=2026-06-01`/`date_to=2026-07-08`, 15/15 bài crawl + phân tích thành công): đối chiếu tay Bảng 3.1 (`VTV=15`)/Bảng 3.13 (`negative=2, neutral=7, positive=6`)/Bảng 3.15 (`Fear=5, Trust=9, Không xác định=1`) với query DB trực tiếp — khớp chính xác 100%. **Xác nhận thật ngoài dự kiến:** bug `emotion="Neutral"` (phát hiện lần đầu ở Slice 3, 1 lần trong mẫu 5 bài) đã **tái xuất hiện thật** ở lần verify này (1/15 bài) — cơ chế validate mới bắt đúng: `emotion=None`, `needs_review=true`, thể hiện đúng ở Bảng 3.15 và `summary_stats`. Xác nhận đây là hành vi lặp lại thật của `qwen3:8b`, không phải lỗi hiếm — validate cần giữ nguyên lâu dài.

## 2026-07-09 — Bỏ dedup xuyên job: implement + verify thật hoàn tất
7 task (migration composite UNIQUE, `_crawl_sources()` dedup nội bộ job, log warning overlap, 5 rule doc, CLAUDE.md, verify thật, regression) đều qua đủ 2 vòng review (spec compliance + code quality), cộng 1 lượt final review toàn bộ diff sau khi xong cả 7 task — bắt được 2 việc bị bỏ sót: (1) log message/tên biến `overlapping_completed_jobs` chưa cập nhật theo đúng scope mở rộng (`completed`/`running`/`pending`); (2) file plan chưa từng commit dù đã dùng để implement. Cả 2 đã sửa (commit `d9b09fc`, `1cb7ec8`). **Kết quả cuối:** 101/101 test pass, verify thật thành công trên DB dev (job mới `9438b4b0...` crawl lại đúng 5/5 URL của job cũ `2324df79...` thay vì bị skip, composite constraint `articles_job_id_url_hash_key` chặn đúng insert trùng trong cùng job, report không rỗng), đã push `main` (14 commit, `c25b26c..1cb7ec8`).

## 2026-07-09 — Bỏ dedup toàn cục theo `url_hash` (quyết định)
Qua thảo luận sâu với user (đọc kết quả verify Slice 3), phát hiện 2 vấn đề cùng gốc rễ (`articles.url_hash` UNIQUE toàn cục): (1) job fail/cancel giữa chừng để lại dữ liệu "mồ côi" không gắn được vào job mới, (2) job mới trùng khoảng ngày với job cũ (dù job cũ **thành công**) bị skip hết, báo `status="completed"` nhưng report **rỗng hoàn toàn**, không cảnh báo. Cân nhắc 5 phương án — user chọn phương án triệt để nhất: **bỏ hẳn dedup xuyên job**, mỗi job luôn crawl + phân tích AI lại từ đầu. **Cập nhật sau code review:** thay vì bỏ hẳn UNIQUE, đổi sang **UNIQUE composite `(job_id, url_hash)`** (migration `0009`) — vẫn đạt đúng mục tiêu nhưng giữ lưới an toàn DB chống trùng trong 1 job. `downgrade()` tự dedupe trước khi tạo lại UNIQUE đơn. Thêm log warning ở `POST /api/reports/create` khi job mới trùng phạm vi ngày/nguồn với job `completed`/`running`/`pending` trước đó. Đánh đổi chấp nhận: tốn AI chạy lại khi job trùng phạm vi, bảng `articles` phình to hơn theo thời gian, kết quả AI có thể khác nhau giữa các lần phân tích cùng 1 bài (non-determinism) — user ưu tiên đúng đắn dữ liệu hơn tiết kiệm tài nguyên. Tác dụng phụ có lợi: tự động bắt được nội dung bài viết đã thay đổi (đính chính/cập nhật) mà không cần cơ chế riêng.

## 2026-07-09 — Bug: VTV 301 redirect khiến crawl lỗi
Khi user tự đọc CSV kết quả verify Slice 3, phát hiện 1 bài VTV bị `status="error"` dù URL vẫn hợp lệ khi truy cập tay — nguyên nhân: VTV trả `301 redirect` sang subdomain khác (`worldcup.vtv.vn`), httpx mặc định không tự theo redirect. Đã thêm `follow_redirects=True` cho `httpx.Client()` ở cả 3 nơi crawl (`article.py`/`listing.py`/`sitemap.py`), 3 test mới, verify bằng dữ liệu thật.

## 2026-07-09 — Slice 3 (AI pipeline đầy đủ) hoàn thành
Thêm cột `article_analysis.ai_model` (migration `0008`) + chuyển `analyze_article()` sang async + thêm `analyze_articles_batch()` (asyncio.Semaphore giới hạn song song qua `AI_CONCURRENCY`, mặc định `1`) + nối vào `report_job.py` + script `export_analysis_csv.py`. **Verify job thật 2 giai đoạn:**
- **Giai đoạn A (smoke test 5 bài, job `2324df79...`):** `source_ids=[VTV, VOV]`, `status=completed`, `crawled=5, analyzed=5`, cả 5 dòng có `ai_model='qwen3:8b'`/`prompt_version=1`.
- **Giai đoạn B (verify chính thức 15 bài, job `32c7bb38...`):** `source_ids=[VTV, VOV, VietnamPlus, CAND, TinGia]`, `crawled=15, analyzed=14` (1 bài lỗi crawl VTV — đúng nhánh `status="error"`), export CSV đọc lướt: topic/sentiment đa dạng theo nội dung, confidence 0.9–0.95.
- **Giới hạn thật:** dù khai 5 `source_ids`, toàn bộ 15 bài crawl được đều từ VTV (nguồn đầu "ăn hết" ngân sách) — không verify được đa dạng nguồn ở bước AI lần này (giải quyết sau bằng `EVEN_DISTRIBUTE_ACROSS_SOURCES`).
- **Phát hiện cần theo dõi:** 1/5 bài AI trả `emotion="Neutral"` — ngoài 6 nhóm chuẩn. Xử lý ở Slice 4 (xem trên).

## 2026-07-08 — Bug: sitemap 403 bị hiểu nhầm thành "không có bài"
Ngay sau khi verify vietnam.vn xong, user tự chạy job qua FE và nhận `completed` với 0 bài — điều tra log phát hiện `GET sitemap.xml` trả `403 Forbidden` (site chặn tạm thời) nhưng `get_article_urls()` không check HTTP status trước khi parse. Đã sửa: dùng `_fetch_with_retry` cho cả fetch index, check `status_code >= 400`, trả về index URL như 1 `failed_loc` → insert `Article(status="error")`. Áp dụng cho mọi nguồn dùng sitemap index. 2 test mới, verify thêm bằng 1 URL 404 thật.

## 2026-07-08 — Thêm nguồn thứ 7: vietnam.vn
Migration `0007`, dùng `_SITEMAP_DATE_PATTERNS`. **Verify job thật thành công** (job `911098e5...`, `date_from=2026-07-07`/`date_to=2026-07-08`): 4/4 bài crawl + phân tích xong, toàn bộ URL thuộc `sitemap-post/2026-07-08.xml`. Nội dung đa dạng (thể thao/giải trí/sức khỏe), không chuyên biệt tin giả — user xác nhận đúng nguồn mong muốn, không lọc chủ đề ở bước crawl.

## 2026-07-08 — tingia.gov.vn: chuyển sang sitemap curated
Chuyển từ listing-page 1 trang sang `parsing_rules.sitemap_pages` (migration `0006`). **Verify job thật thành công** (job `52298372...`): 4/4 bài crawl + phân tích xong, toàn bộ URL thuộc `tin-vua-check.xml`. **Giới hạn:** `MAX_ARTICLES_PER_JOB=4` khiến job dừng trước khi chạm 4 sub-sitemap còn lại — dedup cross-sub-sitemap chưa được test với dữ liệu thật. Phát hiện vận hành: `celery-worker` không tự nạp lại code đã sửa — phải `docker compose restart celery-worker`.

## 2026-07-07 — bocongan.gov.vn: sitemap đóng băng, chuyển sang listing_pages
Sitemap xác nhận đóng băng → chuyển sang crawl 7 trang "chuyên mục" qua `parsing_rules.listing_pages` (selector: `article.card-large`/`a[href^="/bai-viet/"]`/`span.text-bca-gray-700`, `urljoin` href tương đối, fallback `published_at`). Migration `0005` chạy thật trên DB dev. Code đã commit/push `main` (`4756e21`) nhưng **chưa verify bằng job thật** — bị chặn WAF (Incapsula) toàn bộ request từ mạng hiện tại (xác nhận qua curl thường, curl browser header, và `httpx` trực tiếp từ container). Không phải lỗi code — 3 unit test suite pass, chỉ thiếu verify dữ liệu thật do nghẽn mạng ngoài.

## 2026-06-30 — Slice 2: verify job thật VOV
Job thật end-to-end thành công với VOV: 4/4 bài crawl + AI phân tích xong, `.docx`/`.json` hợp lệ.

## 2026-06-29 — Crawl4AI engine
Engine fetch thay thế httpx, bật theo nguồn qua `parsing_rules.engine`, không đổi hành vi nguồn không khai engine (VTV).

## 2026-06-25 — Slice 0 + Slice 1 (walking skeleton) + mở rộng
`docker-compose up` chạy đủ 7 service (healthy), DB đủ 5 bảng, Celery + Ollama hoạt động. Slice 1: 104 bài crawl thật từ VTV, AI phân tích thật qua `qwen3:8b`, DOCX/JSON hợp lệ, 32 unit test pass. Mở rộng cùng đợt (merge `main`): bảng crawl trực tiếp + benchmark, Cancel, giới hạn `MAX_ARTICLES_PER_JOB`, khôi phục job sau F5, fix AI timeout/crawler error-handling.

## (trước đó) — Pilot test thật
Khảo sát 40 kênh, crawl được 11/40 — làm nền cho quyết định "không build social media" trong MVP.
